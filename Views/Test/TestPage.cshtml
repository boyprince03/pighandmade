@{
    ViewData["Title"] = "權限測試";
}

<div class="container mt-5 text-center">
    <h2 class="text-success">✅ 測試通過</h2>
    <p class="lead">這個頁面角色為：<strong>@ViewBag.Role</strong></p>
    <a class="btn btn-primary mt-3" href="/">回首頁</a>
</div>
